import BaseLayout from '@/layouts/Base';
import { Contact } from '@/partials';

const contact = () => {
  return (
    <BaseLayout>
      <Contact />
    </BaseLayout>
  );
};

export default contact;
